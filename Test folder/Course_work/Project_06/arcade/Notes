// function endGame() {
//     if (gameState.snake.head[0] == gameState.snake.body[gameState.snake.body.length - 1][0] && gameState.snake.head[1] == gameState.snake.body[gameState.snake.body.length - 1][1]) {
//         clearInterval(snakeInterval);
//         document.getElementById('best-score').innerHTML = 'Best: ' + score;
//         alert('Game over');
//     } else if (gameState.snake.head[0] == -1 || gameState.snake.head[1] == -1) {
//         clearInterval(snakeInterval);
//         document.getElementById('best-score').innerHTML = 'Best: ' + score;
//         alert('Game over');
//     } else if (gameState.snake.head[0] == 15 || gameState.snake.head[1] == 15) {
//         clearInterval(snakeInterval);
//         document.getElementById('best-score').innerHTML = 'Best: ' + score;
//         alert('Game over');
//     }
// }

// reset button and call build initial state
// unshift apple into the body
// increase length of body when we eat the apple

// function deleteBoard() {
//     for (let x = 0; x < 15; x++) {
//         const row = document.removeChild('id');
//         for (let y = 0; y < 15; y++) {
//             const column = document.removeChild('id');
//             row.removeAttribute(column);
//         }
//         table.removeAttribute(row);
//     }
//     board.removeAttribute(table);
// }

// reset.onclick = () => {
//     gameState.gameReset = false;
//     buildInitialGameState();
// }

// function buildInitialGameState() {
//     deleteBoard();
//     deleteSnake();
//     deleteApple();
//     makeBoard();
//     drawSnake();
//     drawApple();
// }